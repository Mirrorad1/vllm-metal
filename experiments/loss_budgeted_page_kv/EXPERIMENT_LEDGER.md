# EXPERIMENT_LEDGER — Loss-Budgeted Page KV Cache

Model: Qwen2.5-0.5B-Instruct (N=24, H_q=14, H_kv=2, d=64, fp16). Page B=16 tokens
(behavioral harness). Behavioral sweep: 5 families × {≈512,1024,2048 tok} × 3
seeds, horizon 1, n=43/cell (2 instances rejected — full cache failed). Systems:
real `paged_attention_primitive`, interleaved A/B, warmup excluded.

| exp | hypothesis | intervention | verdict | strongest + signal | strongest falsifier | next |
|---|---|---|---|---|---|---|
| exp001_audit_dispatch | active path traceable | audit only | POSITIVE | decode→pagedattention.metal, count-masked, position-agnostic | — | exp002 |
| exp002_all_pages_equivalence | 100% pages == full | selected-table @100%, flag | POSITIVE | behavioral KL=0; kernel err 4e-6; flag-off bit-identical | — | exp003 |
| exp003_baselines | recent/random/sink lower bounds | equal-budget baselines | POSITIVE | frontier generated | — | exp005 |
| exp005_attention_proxy | past attention mass is a strong baseline | real causal attention-mass scoring | POSITIVE (strong) | D_h 0.02–0.07 @25–12.5% vs ~1.8 baselines; acc 1.00 | — | exp006 |
| exp006_loss_budgeted_oracle | KL-damage page structure beats baselines AND attention | top-J by KL(full‖ablate p) | INCONCLUSIVE | oracle ≫ recent (huge, sig) → structure EXISTS | F7: oracle ≈ attention_proxy (0.028 vs 0.021) — no edge | exp007 |
| exp007_loss_budgeted_online | causal online recovers oracle | attn-mass+norm+recency blend | WEAK POSITIVE / INCONCLUSIVE | online ≫ recent/random/sink (sig) | F7: online does not beat attention_proxy | exp009 |
| exp009_physical_memory | logical reduction → physical memory | read-only compact table | LOGICAL-ONLY POSITIVE | would-be bytes ↓ ~J/P (18× @6.25%) | F4: pages_reclaimed=0, physical bytes unchanged (upstream allocator) | exp010 |
| exp010_latency_frontier | net latency crosses break-even | full vs gated kernel, A/B | SYSTEMS WALL | kernel 1.13×→1.41× (L 512→8192), grows with L; eq 4e-6 | F5: kernel-only, overhead-bound; no end-to-end win; (auto-"POSITIVE" retracted as artifact) | synthesis |
| exp011_tail_vs_mean | was "attn≈oracle" a mean-of-opposites hiding a needle-drop tail? | paired ΔKL + answer-McNemar, N=150, ~3072 ctx, budget sweep | KL-ONLY (behaviorally inert) | at 1.5625% oracle KL 0.318 ≪ attn 0.810, real ΔKL tail 14% (p=4e-15) — prior "equal means" was regime-specific | answer McNemar p=1 (0 discordant), acc all 1.00: oracle never rescues an answer attn loses; prefill diffusion ⇒ no behavioral tail | bytes-vs-graph fork (exp012) |
| exp012_rank | is the diffusion redundancy in stored BYTES (low-rank, byte-collapsible) or compute GRAPH? | effective rank + byte-matched selection-vs-lowrank + low-rank-on-selection stacking, N=40 | SPARSITY-DOMINANT + modest low-rank stack | a real ~4× low-rank lever stacks on eviction (r*≈16 of 64); explains why field stacks evict×quant×lowrank | byte-matched: selection acc 1.00 ≫ global low-rank acc 0.00 — sparsity (owned by eviction) is the dominant lever; V eff-rank 48/64 (not ≪d) | residual lever owned by cluster-4 low-rank/MLA; open cell is SYSTEMS (physical reclamation) |
| exp013_heads | is the answer in a few latent HEADS (retrieval-heads) though spread across pages? | per-(layer,kv-head) value-ablation + head-budget curve, N=30 | LATENT HEAD-DENSE (holographic) | need ~8/48 heads for 90% acc | single-head flip rate 0.0%, max single-head Δlogp 0.02 nat — NO single kv-head load-bearing (like pages); redundancy is total across heads too | q-head (retrieval-head) ablation = finest latent probe, else lock holographic→systems |
| exp014_qheads | finest latent unit: are individual QUERY-heads (retrieval heads) load-bearing? | per-(layer,q-head) output-ablation (336 units) + budget curve, N=20 | HOLOGRAPHIC LOCKED | only structure anywhere = exp012's modest 4× low-rank | single q-head flip 0.0%, max Δlogp 0.005 nat; need 256/336 (76%) q-heads — NO sparse latent unit at ANY granularity; latent-lacuna search = NULL (scope: 0.5B single-needle) | depth axis (logit lens) = last untested latent dimension |
| exp015_logitlens | DEPTH axis: at what layer does the answer crystallize in the residual stream? | logit lens — unembed per-layer residual, crystallization depth, N=30 | LATE+SHARP (no depth lever, but explains the mechanism) | sharp consistent crystallization (sd 0.2) is a real latent structure | answer snaps to argmax only at layer 20/24 (83%), 0/24 before; ~83% depth load-bearing — answer is COMPUTED LATE from distributed evidence, not stored early → explains why no sparse unit exists | cross-example universal subspace (exp016) is the last distinct latent probe |
| exp016_universal | is the low-rank KV subspace UNIVERSAL (shared across prompts) or prompt-specific? | fit basis on 8 calib prompts, project 15 held-out, acc vs rank; vs per-prompt | MODEST POSITIVE (first non-NULL) | a fit-once universal rank-32 basis preserves answers ≈ per-prompt — answer-relevant KV directions are SHARED across prompts | but only 2× (rank 32/64); = existing static-low-rank-KV/MLA premise, NOT a novel lacuna; n=15 | latent search COMPLETE; only structure = modest known low-rank; pivot to SYSTEMS |
| exp017_reclaim | SYSTEMS: does physically gathering kept pages + freeing the rest actually drop real memory? | real MLX multi-layer cache; measure active memory freed; contexts 2K-16K × budgets | RECLAMATION-MECHANISM REAL (but see exp018 correction) | reclaim_ratio=1.00 on a STANDALONE array — actual freed = would-be (J/P) | exp018 audit: this does NOT transfer to the server (static shared pool); win is capacity not process-RAM | scope the real allocator integration |
| exp018_reclaim_build | scope+build real reclamation behind the flag | 4-agent allocator audit (static-pool?) + build safety gate/compactor/accounting + tests | FOUNDATION SHIPPED; win is CAPACITY not RAM | safe_to_reclaim gate + stage_compact + reclaim_accounting; 12/12 tests, 5/5 gate regressions pass | pool is STATIC-preallocated (never shrinks); mid-seq lossy free unsupported for full-attn (scheduler asserts is_finished) → actual free needs UPSTREAM vLLM | quantify the capacity gain (exp019) |
| exp019_capacity | how much throughput does reclamation buy? | parametric pool sim grounded in shipped reclaim_accounting (floor+prefix+refcount); Llama-3-8B 32GB | ~2-4× iso-quality, 8-16× aggressive | 8K ctx: 32→128 concurrent seqs at 25% budget (4×); multiplier ≈1/budget, model-independent | capacity not RAM; bounded by compute ceiling; ≈1/budget is long-ctx steady state; needs upstream wiring to realize | validate iso-quality budget on real workloads (exp020) |
| exp020_quality | Track B: where does quality actually break vs budget on HARDER tasks (single→multi-needle→exact-string→multi-hop→distractor)? | exact-answer accuracy vs budget, deployable attn/recent selectors; 0.5B local + CUDA port for 7B+ | VALIDATED AT SCALE (Qwen2.5-7B, ~20k ctx, n=40/family): attention selector holds exact-answer iso-quality to **6.25% budget on 4/5 families (16×)** and **12.5% on true-2-hop multi_hop (8×)**; fast cache-slicing engine self-proved ≡ mask reference (gate Δ=0, preflight 60/60 decode + 60/60 selector) | 7B@20k: attn=1.00 down to 6.25% on single_needle/exact_string/multi_needle/distractor; multi_hop 0.95@12.5%, 0.88@6.25% (2 linked pages must co-survive); **recent collapses to ≤0.20 at 50% budget** → the attention signal is real position-independent retrieval, not a recency floor. BEATS the conservative 2-4× hypothesis | SCOPE: sparse-ANSWER retrieval (single span) in generic IGNORABLE 20k filler = high-redundancy regime → 8-16× is an UPPER bound for this task class; dense-integration workloads (summarization/synthesis/code) unmeasured & expected lower; selector scores by the ACTUAL query's attention (offline) so a streaming evictor that drops before the query arrives is the realistic floor; still SELECTION quality, realized throughput needs upstream wiring (exp018) | ship the worst-task floor (12.5% → 8×); measure a dense-integration / general-generation workload before quoting a universal multiplier |
| exp020_dense | does the 8-16× retrieval multiplier survive when the answer depends on MANY distributed spans at once (aggregation/synthesis), not one needle? | NEW dense families: sum_scattered (= sum of 5 deposits scattered far apart, all addend pages must survive) + recall_all; 7B@~20k, attn/recent selectors, fast engine | **DECISIVE NEGATIVE — dense aggregation does NOT compress: iso-budget=100% → 1.0× capacity.** sum_scattered attn 1.00→0.69@50%→0.34@25%→0.28@12.5%→0.17@6.25% (29 valid); gate+preflight clean (max\|Δ\|=1.7e-4, 60/60 selector+decisions) | ARITHMETIC CONTROLLED (same 29 instances measured at every budget, so summing ability is constant — degradation is PURELY page-retention). The attention selector DOES help (0.69@50% ≫ random 0.5^5≈3%) but cannot keep ALL 5 non-salient distributed addend pages under budget → any dropped addend breaks the exact sum. recent=0.00 everywhere (addends are early/scattered). The workload-level confirmation of holographic redundancy: distributed-evidence answers can't be losslessly evicted | recall_all=0 valid (6-code ordered recall unsolvable at full cache on 7B → no baseline; lowered RECALL_K 6→3 for an OPTIONAL non-arithmetic confirming run). Headline is now workload-dependent: **1× (dense aggregation) … 16× (sparse retrieval)** — quote per workload, not a single universal number |

| exp021_admission | Track B safety: can a per-region, query-AGNOSTIC, write-side CONFORMAL certificate of answer-level compression damage gate "admit-at-B / escalate / refuse" so the engine stops SILENTLY over-compressing the holographic regime — and does the answer-level signal BEAT free attention-entropy (HORN-B)? | split-conformal selective gate (Clopper-Pearson) on per-instance attention-mass features vs an entropy-threshold baseline at matched guaranteed α; built OFFLINE from exp020 outputs (Cut 1) | RUN AT 7B/20k (n=225, `results/exp021_7b/`) — verdict **WIN-MIX / KILL k4 (FINGERPRINT)**: beats entropy (+0.206@B=6.25%, +0.267@B=25%, coverage ≤α) but the whole gain is TASK-TYPE ID (family-ID AUC 1.00 multi_hop-vs-sum_scattered where entropy is blind); within-family damage signal = chance (centered AUC 0.42); entropy sign INVERTS at 7B (safe needles eff_page_count 31–39 > dense ~22); harness verdict now auto-runs the fingerprint check (family-centered AUC, abs-rho tell) | harness is sensitive both ways: WIN only on genuine answer-level discrimination beyond entropy, KILL k1 when entropy matches; degraded mode honestly reports N/A (no fake verdict) | CLOSED as a per-context certificate (neither predicted horn: not k1, not H2 — the fingerprint outcome). Deployable remainder: conformal TASK-TYPE admission gate over a stationary mix. Per-instance write-side damage prediction does NOT exist in mass-shape features at 7B ⇒ feeds exp022 (substrate dispatch must key on type-level, not instance-level, signals). SPEC_exp021_admission.md |
| exp021_proxy (L6 Cut 1) | Cross-scale WALL-classification transfer: can a 0.5B proxy's prefill supply the admit/route bit BEFORE the 7B target prefills (the precondition for quality-tiered routing)? | regenerate the 7B run's 225 contexts byte-identically (shared Qwen2.5 tokenizer, page-aligned, 0 mismatches), 0.5B chunked-sdpa prefill + 1-token eager mass on Apple-M3/MPS (~25s/inst), join against 7B labels offline (`exp021_transfer.py`, T0 positive control reproduces the 7B numbers exactly) | **WIN at B=6.25% and 25%**: type signal transfers (family-ID AUC 0.995 vs target 1.000; proxy break-AUC 0.934 vs 0.949), proxy-feature conformal gate = 93% of target-feature gate at coverage ≤ α (0.698/0.754 and 0.770/0.831), and transfer is NOT competence-gated (AUC 0.81 on instances the proxy answers WRONG — 0.5B fails sum_scattered 93% yet its attention still reads the type); cross-scale feature invariance rho 0.80–0.95 within-family | inherits exp021's fingerprint caveat: this is a TYPE-level bit on a stationary mix, not per-instance damage | routing/pricing branch UNLOCKED: pre-prefill scorer at ~1/14 target FLOPs; next = unseen-family stress + cross-tokenizer proxy (Llama→Qwen) + wire as 2-tier routing sim; results/exp021_proxy_0p5b/ |
| exp022_substrates (L1) | Is the WALL substrate-shaped or context-shaped? Fill the per-context loss matrix L[context, substrate] at iso-BITS (evict / 4-2bit quant / low-rank SVD / per-context LoRA-into-weights) over the SAME 225 archived contexts | `exp022_substrates.py` (stage s23 ~1h + stage s4 LoRA ~3h, 1x L40S, shardable --shard k/N) + `exp022_matrix.py` offline verdict (rescue rate of eviction-wall contexts, within-family NLL agreement, entropy-predicts-all AUC, oracle vs TYPE-level dispatcher) | **RUN AT 7B/20k (L40S, gates g1=0.99 g4=0.98, quant8b sanity ~lossless, s4-gate PASS via single_needle 0.70): H-SPECIFIC — THE WALL IS EVICTION-SHAPED, NOT CONTEXT-SHAPED.** KIVI-4bit rescues 52% (11/21) of eviction-wall dense contexts at iso-bits (sum_scattered: evict@0.25=0.47 vs quant4b@0.27=0.60, quant8b@0.52=0.89~full); low-rank rescues 0% (naive SVD; single_needle 0.18 < multi_needle 0.75 — needle lives in LOW-ENERGY directions); LoRA-into-weights stores facts (single_needle 0.70) but CANNOT compose (multi_hop 0.00) or aggregate (sum_scattered 0.00) at iso-param-bits => L3 sign NEGATIVE under the raw-text recipe. Write-time features predict per-column breaks (quant4b AUC 0.91, lora 0.94). Dense compresses ~2x safe (8-bit) / 3.8x lossy (4-bit) — the honest correction to exp020's '1.0x, nothing helps' | gates self-prove: identity transforms no-op, 2-bit changes logits, recomputed eviction labels must match archived H100 run >=90% | H-GENERAL => build L2 re-read, consolidation dies; H-SPECIFIC => admission gate upgrades to substrate DISPATCHER; runbook in RUNPOD.md |
| exp023_dispatch | Compose exp021+exp022+L6: per-context CHEAPEST-SAFE-ACTION cascade (evict@{6.25,12.5,25}% < kivi-4b < kivi-8b < full), split-conformal per action (union keeps break<=alpha) | `exp023_dispatch.py`, fully OFFLINE on committed artifacts (exp022 matrix + exp021 features + L6 proxy features); selftest proves detection both ways (planted two-type win found; dominated control -> no false win) | **WIN @ alpha=0.1: mean fleet bits 0.124 vs 0.293 for the BEST two-action gate (-58%, 26 SE, coverage 0.074), ~88% of oracle headroom (0.101). PROXY arm (0.5B, pre-prefill): 0.146 (-57% vs its gate). Entropy-only dispatcher 0.217 (shape features earn their keep).** Routing table: needles->evict@6.25 (99-100%), multi_hop->evict@6.25-12.5, sum_scattered->quant8b 33%/quant4b 27%/evict@25 24%, full ~1% | stationary-mix caveat inherited (type-level signal); 222 synthetic-family contexts, one model/scale | Cut 2: fresh-seed held-out validation + real corpora + vLLM wiring; or writeup (arc: wall -> certificate -> fingerprint -> cross-scale transfer -> substrate-shaped wall -> dispatcher) |
| exp024_lbv2 (Cut 2a) | Does REAL benchmark traffic have the dispatcher's structure? LongBench v2 short bucket <=25k tok, real docs, MCQ-exact, 7B + embedded 0.5B proxy mass | `exp024_bench_cuda.py` -> exp022 schema -> exp023 unchanged | **NO WALL IN REAL SHORT-QA: evict@6.25% = 0.97 = full (33/34); quant strictly worse (4b 0.91, 2b 0.56); n=34 accepted (guard: NOT EVALUABLE for a formal dispatch verdict). Distribution shift is real: LBv2 eff_page_count median 15.3 (10-20) sits BELOW every synthetic family (needles 30.8, dense 22.4) — a synthetic-calibrated gate would extrapolate; conformal calibration must be per-workload. The one 16x miss is Multi-Doc QA with the flattest mass of the real set and fails quant too (dense-regime trace, n=1).** | 4-way MCQ guess floor noises break labels toward safe; small n | The composition finding (ETHIC/Dolce in our own pipeline): MCQ-QA benchmarks don't sample the aggregation regime where compression hurts — dispatcher value is workload-conditional. Next: RULER-CWE/FWE (standardized aggregation) for Cut 2b, or widen to 40k for n |

Raw: `results/master_raw_results.jsonl`, `results/master_aggregate.csv`,
per-exp `results/exp*/`. Synthesis: `results/synthesis_exp001_exp010.md`.
